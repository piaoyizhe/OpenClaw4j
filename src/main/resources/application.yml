# OpenClaw配置文件
openclaw:
  # 记忆模式配置
  memory:
    enabled: true
    storage:
      directory: ./memory
      file_format: markdown
    # 日志配置
    logs:
      enabled: true
      daily_log:
        enabled: true
        cron_expression: "0 0 22 * * ?"  # 每天22:00执行
        archive_days: 90  # 日志归档天数
        clean_days: 180  # 日志清理天数
      weekly_report:
        enabled: true
        cron_expression: "0 0 22 ? * 6"  # 每周六22:00执行
      monthly_report:
        enabled: true
        cron_expression: "0 0 22 1 * ?"  # 每月1日22:00执行
    # SQLite配置
    sqlite:
      enabled: true
      db_path: ./memory/openclaw.db
      optimize_interval: 24  # 数据库优化间隔（小时）
    # 搜索配置
    search:
      enabled: true
      vector_weight: 0.7  # 向量搜索权重
      text_weight: 0.3   # 文本搜索权重
      default_max_results: 10  # 默认最大结果数
      default_min_score: 0.1   # 默认最小评分
  # 大模型配置
  llm:
    provider: glm
    model: glm-4.7
    api_key: d0da4f51bb284e5c950c1682a9b1f385.MTqFiIW2yO0uLvkk
    api_url: https://open.bigmodel.cn/api/paas/v4/chat/completions
    timeout: 30000
  # 控制台配置
  console:
    prompt: "OpenClaw> "
    welcome_message: "欢迎使用OpenClaw记忆模式！输入您的问题，或输入 'help' 查看可用命令，输入 'exit' 退出程序。"
    error_message: "抱歉，处理您的请求时出现错误。"
  # 文件索引配置
  file_indexer:
    enabled: true
    chunk_size: 1600  # 分块大小（字符）
    overlap_size: 80   # 重叠大小（字符）
    watch_enabled: true  # 是否启用文件监控
  # 邮件配置
  mail:
    enabled: true
    host: smtp.qq.com
    port: 465
    username: 591543227@qq.com
    password: tgpskgbbshqzbcba
    from: 591543227@qq.com
    ssl: true
    timeout: 30000
